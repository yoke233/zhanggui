# IGI（归档）

本目录保存早期关于 **IGI v1** 的资源模型与 API 草案。

当前仓库主线已采用更轻量的 v1 落地：以文件系统约定（`FILE_STRUCTURE.md`）+ Bundle（`packs/{pack_id}`）+ `ledger/events.jsonl` 作为审计与验收真相源，因此 IGI 文档暂不作为“必读/必实现”内容。

需要再次启用 IGI 时，可以：
- 将本目录文档作为参考输入，逐步把现有落盘字段映射为 IGI 资源（或生成兼容视图），避免一次性引入全量对象模型。
