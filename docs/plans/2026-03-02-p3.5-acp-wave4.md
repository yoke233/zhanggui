# Wave 4 — Workspace 收口 + 术语统一 + 全量回归

> **Wave Goal:** 完成冗余通信抽象清理、Workspace 插件收口、`remote_url` 单输入模式落地，并统一文档与代码术语（`review_orchestrator`）。

## 任务列表

### Task W4-T1: 冗余通信抽象清理

**Files:**
- Modify: `internal/core/agent.go`
- Modify: `internal/core/plugin.go`
- Modify: `internal/core/bootstrap.go`
- Test: `internal/core/*_test.go`

**Depends on:** `[]`

**Step 1: Write failing test**
```go
func TestCoreNoRedundantCommunicationAbstractions(t *testing.T) {
    // 编译期保证核心层仅保留 ACP + RoleResolver 路径
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/core/ -run TestCoreNoRedundantCommunicationAbstractions -count=1`
Expected: `legacy reference still exists`

**Step 3: Minimal implementation**
- 清理核心层冗余通信抽象与未使用字段
- `BootstrapSet` 仅保留 role-driven 调用所需对象

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/core/... -count=1 -v`
Expected: `PASS`

---

### Task W4-T2: Executor 收口为 role-only 执行路径

**Files:**
- Modify: `internal/engine/executor.go`
- Modify: `internal/engine/executor_test.go`

**Depends on:** `[W4-T1]`

**Step 1: Write failing test**
```go
func TestExecutorRoleOnlyPath(t *testing.T) {
    // NewExecutor 只接受 RoleResolver + ACPClient provider
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/engine/ -run TestExecutorRoleOnlyPath -count=1`
Expected: `old path still reachable`

**Step 3: Minimal implementation**
- 删除 executor 中非 role 驱动分支
- 统一 stage 执行为 `stage.role -> RoleResolver -> ACP`

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/engine/... -count=1 -v`
Expected: `PASS`

---

### Task W4-T3: workspace-worktree 插件收口

**Files:**
- Modify: `internal/plugins/workspace-worktree/worktree.go`
- Modify: `internal/plugins/workspace-worktree/worktree_test.go`
- Modify: `internal/plugins/factory/factory.go`

**Depends on:** `[W4-T2]`

**Step 1: Write failing test**
```go
func TestWorktreePluginRegisteredAndUsed(t *testing.T) {
    // factory 注册成功，executor 可调用 setup/cleanup
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/plugins/workspace-worktree/... -count=1`
Expected: `registration mismatch`

**Step 3: Minimal implementation**
- 确保 worktree setup/cleanup 由插件独立承载
- executor 不再内联重复实现

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/plugins/workspace-worktree/... -count=1 -v`
Expected: `PASS`

---

### Task W4-T4: workspace-clone `remote_url` 单输入模式

**Files:**
- Modify: `internal/plugins/workspace-clone/clone.go`
- Modify: `internal/plugins/workspace-clone/clone_test.go`
- Modify: `internal/web/handlers_project.go`

**Depends on:** `[W4-T3]`

**Step 1: Write failing test**
```go
func TestCloneByRemoteURL(t *testing.T) {
    // SSH/HTTPS URL 均可
}

func TestCloneRejectsInvalidRemoteURL(t *testing.T) {
    // 非法 URL 直接报错
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/plugins/workspace-clone/... -count=1`
Expected: `remote_url path incomplete`

**Step 3: Minimal implementation**
- `WorkspaceOpts.RemoteURL` 作为 clone 唯一输入
- 支持 SSH/HTTPS
- 非法 URL 返回可诊断错误
- 项目导入 API 只接收 `remote_url`

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/plugins/workspace-clone/... -count=1 -v`
Expected: `PASS`

---

### Task W4-T5: 术语与文档收口（review_orchestrator）

**Files:**
- Modify: `docs/spec/*.md`
- Modify: `docs/plans/2026-03-02-p3.5-acp-*.md`

**Depends on:** `[W4-T2]`

**Step 1: Write failing test**
```text
（文档一致性检查）
```

**Step 2: Run to confirm failure**
Run（legacy 术语探测）: `rg -n 'review_panel|change_agent|implement_agent' docs/spec docs/plans -g '*.md'`
Expected: `非 0 结果`

**Step 3: Minimal implementation**
- 术语统一为 `review_orchestrator`
- 动作统一为 `change_role`
- 字段统一为 `implement_role` / `stage.role`

**Step 4: Run tests to confirm pass**
Run（legacy 术语清零）: `rg -n 'review_panel|change_agent|implement_agent' docs/spec docs/plans -g '*.md'`
Expected: `0 结果`

---

### Task W4-T6: 全量回归

**Files:**
- (无新增文件)

**Depends on:** `[W4-T1, W4-T2, W4-T3, W4-T4, W4-T5]`

**Step 1: Write failing test**
```text
（无新测试，执行全量回归）
```

**Step 2: Run to confirm failure**
Run: `go build ./...`
Expected: `如失败则修复后重试`

**Step 3: Minimal implementation**
- 修复收口阶段产生的编译/测试问题
- 确保代码与文档口径一致

**Step 4: Run tests to confirm pass**
Run:
```bash
go build ./...
go test ./... -count=1
go vet ./...
```
Expected: `全部 PASS`

---

## Risks and Mitigations

| Risk | Severity | Mitigation |
|------|----------|------------|
| 收口清理引入回归 | 高 | W4-T6 全量回归与 smoke 兜底 |
| workspace 逻辑迁移导致行为漂移 | 中 | 对照插件测试 + executor 集成测试 |
| 术语改名不完整导致文档混乱 | 中 | W4-T5 用 rg 全局收口 |

## Wave Exit Gate
- Execute mandatory gate sequence via `executing-wave-plans`.
- Wave-specific acceptance:
  - [ ] Executor 仅保留 role-driven ACP 执行路径
  - [ ] workspace-worktree 插件独立可用
  - [ ] workspace-clone 支持 `remote_url` 单输入（SSH/HTTPS）
  - [ ] 文档术语统一为 `review_orchestrator` / `change_role` / `implement_role`
- Wave-specific verification:
  - [ ] `go build ./...` — PASS
  - [ ] `go test ./... -count=1` — PASS
  - [ ] `go vet ./...` — PASS
  - [ ] `go test ./internal/plugins/workspace-clone/... -count=1 -v` — PASS
  - [ ] `rg -n 'review_panel|change_agent|implement_agent' docs/spec docs/plans -g '*.md'`（legacy 术语）— 0 结果

## Next Wave Entry Condition
- 本 Wave 为 ACP Role-driven 计划最终波次。全部 Gate 通过后该计划完成。
