# Wave 1 — ACP + Role Core

> **Wave Goal:** 产出生产级 `internal/acpclient/` 并落地 RoleResolver 基础能力，为后续 role-driven 调用提供统一底座。

## 任务列表

### Task W1-T1: ACP 协议类型定义（protocol.go）

**Files:**
- Create: `internal/acpclient/protocol.go`
- Create: `internal/acpclient/protocol_test.go`

**Depends on:** `[]`

**Step 1: Write failing test**
```go
func TestPromptRequestMetadataIncludesRole(t *testing.T) {
    req := PromptRequest{SessionID: "s1", Prompt: "hello", Metadata: map[string]string{"role_id": "worker"}}
    params := req.ToParams()
    require.Equal(t, "worker", params.Metadata["role_id"])
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/acpclient/ -run TestPromptRequestMetadataIncludesRole -count=1`
Expected: `undefined: PromptRequest`

**Step 3: Minimal implementation**
定义并测试以下核心类型：
- `LaunchConfig`
- `ClientCapabilities`
- `NewSessionRequest` / `LoadSessionRequest` / `SessionInfo`
- `PromptRequest` / `PromptResult` / `TokenUsage`
- `CancelRequest`
- fs / permission / terminal 工具请求与响应类型

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/acpclient/ -run TestPromptRequestMetadataIncludesRole -count=1`
Expected: `PASS`

---

### Task W1-T2: JSON-RPC Transport（transport.go）

**Files:**
- Create: `internal/acpclient/transport.go`
- Create: `internal/acpclient/transport_test.go`

**Depends on:** `[W1-T1]`

**Step 1: Write failing test**
```go
func TestTransportConcurrentCalls(t *testing.T) {
    // 并发 20 个 Call，确保 request/response 不串线
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/acpclient/ -run TestTransportConcurrentCalls -count=1`
Expected: `undefined: NewTransport`

**Step 3: Minimal implementation**
实现：
- `Call(ctx, method, params)`
- `Notify(method, params)`
- 后台 `readLoop()`
- pending request 映射与超时/取消清理
- inbound 消息分发（session/update、tool call）

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/acpclient/ -run TestTransport -count=1 -race`
Expected: `PASS`

---

### Task W1-T3: Handler 接口定义（handler.go）

**Files:**
- Create: `internal/acpclient/handler.go`
- Create: `internal/acpclient/handler_test.go`

**Depends on:** `[W1-T1]`

**Step 1: Write failing test**
```go
func TestNopHandlerImplementsHandler(t *testing.T) {
    var h Handler = &NopHandler{}
    require.NotNil(t, h)
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/acpclient/ -run TestNopHandlerImplementsHandler -count=1`
Expected: `undefined: Handler`

**Step 3: Minimal implementation**
定义 `Handler` 与 `EventHandler`：
- 文件读写回调
- 权限回调
- terminal/* 回调
- `NopHandler` 空实现

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/acpclient/ -run TestNopHandlerImplementsHandler -count=1`
Expected: `PASS`

---

### Task W1-T4: Client 生命周期（client.go）

**Files:**
- Create: `internal/acpclient/client.go`
- Create: `internal/acpclient/client_test.go`
- Create: `internal/acpclient/testdata/fake_agent.go`

**Depends on:** `[W1-T2, W1-T3]`

**Step 1: Write failing test**
```go
func TestClientLifecycle(t *testing.T) {
    c := newTestClient(t)
    require.NoError(t, c.Initialize(ctx, ClientCapabilities{FSRead: true, FSWrite: true, Terminal: true}))
    sess, err := c.NewSession(ctx, NewSessionRequest{CWD: t.TempDir()})
    require.NoError(t, err)
    _, err = c.Prompt(ctx, PromptRequest{SessionID: sess.SessionID, Prompt: "hello", Metadata: map[string]string{"role_id": "worker"}})
    require.NoError(t, err)
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/acpclient/ -run TestClientLifecycle -count=1`
Expected: `undefined: New`

**Step 3: Minimal implementation**
实现公开 API：
- `New`
- `Initialize`
- `NewSession`
- `LoadSession`
- `Prompt`
- `Cancel`
- `Close`

要求：
- `Prompt` 支持流式 `session/update` 回调
- 工具调用通过 `Handler` 路由
- `Close` 幂等并确保子进程回收

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/acpclient/ -run TestClient -count=1 -v`
Expected: `PASS`

---

### Task W1-T5: RoleResolver 基础（role_resolver.go）

**Files:**
- Create: `internal/acpclient/role_resolver.go`
- Create: `internal/acpclient/role_resolver_test.go`

**Depends on:** `[W1-T1]`

**Step 1: Write failing test**
```go
func TestResolveRoleValidatesCapabilitySubset(t *testing.T) {
    // role.capabilities 不能超过 agent.capabilities_max
}

func TestResolveRoleReturnsProfiles(t *testing.T) {
    // role_id -> AgentProfile + RoleProfile
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/acpclient/ -run TestResolveRole -count=1`
Expected: `undefined: RoleResolver`

**Step 3: Minimal implementation**
实现：
- `type RoleResolver struct { ... }`
- `Resolve(roleID string) (AgentProfile, RoleProfile, error)`
- 能力上限子集校验
- 缺失 role / agent 的可诊断错误

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/acpclient/ -run TestResolveRole -count=1`
Expected: `PASS`

---

### Task W1-T6: 集成冒烟测试

**Files:**
- Create: `internal/acpclient/integration_test.go`

**Depends on:** `[W1-T4, W1-T5]`

**Step 1: Write failing test**
```go
func TestFullLifecycleWithRoleMetadata(t *testing.T) {
    // New -> Initialize -> NewSession -> Prompt(role_id=worker) -> Close
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/acpclient/ -run TestFullLifecycleWithRoleMetadata -count=1`
Expected: `test failure`

**Step 3: Minimal implementation**
- 用 fake agent 补齐完整生命周期路径
- 覆盖 tool call 回调与 session/update 回调

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/acpclient/... -count=1 -v -race`
Expected: `PASS`

---

## Risks and Mitigations

| Risk | Severity | Mitigation |
|------|----------|------------|
| Transport 并发竞态 | 高 | 并发 + race 测试强制覆盖 |
| Fake agent 行为偏差 | 中 | 仅模拟协议最小闭环，避免过度模拟 |
| RoleResolver 校验缺失导致越权 | 高 | capability subset 测试必须通过 |

## Wave Exit Gate
- Execute mandatory gate sequence via `executing-wave-plans`.
- Wave-specific acceptance:
  - [ ] `internal/acpclient` 提供完整公开 API
  - [ ] `RoleResolver` 可完成 role 解析与能力校验
  - [ ] fake agent 可支撑单测与集成测试
- Wave-specific verification:
  - [ ] `go test ./internal/acpclient/... -count=1 -v -race` — PASS
  - [ ] `go build ./...` — PASS
  - [ ] `go vet ./internal/acpclient/...` — PASS

## Next Wave Entry Condition
- Governed by `executing-wave-plans` verdict rule (`Go` / satisfied `Conditional Go` only).
