# Wave 3 — Secretary / Review Orchestrator Role化

> **Wave Goal:** 让 Secretary 与 Review Orchestrator 全量按 role 调用 ACP，完成持久会话恢复、MCP 查询工具、文件变更回调与 Web 适配。

## 任务列表

### Task W3-T1: Secretary 持久会话按 role 运行

**Files:**
- Modify: `internal/secretary/agent.go`
- Modify: `internal/secretary/agent_test.go`

**Depends on:** `[]`

**Step 1: Write failing test**
```go
func TestSecretaryUsesBoundRole(t *testing.T) {
    // role_bindings.secretary.role 生效
    // Start 时优先 LoadSession，失败回退 NewSession
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/secretary/ -run TestSecretaryUsesBoundRole -count=1`
Expected: `missing role-bound startup path`

**Step 3: Minimal implementation**
- Secretary 启动读取 `role_bindings.secretary.role`
- RoleResolver 解算 `AgentProfile + RoleProfile`
- 会话恢复顺序：`LoadSession -> NewSession`
- 持久化 `agent_session_id`（供重启恢复）

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/secretary/ -run TestSecretaryUsesBoundRole -count=1`
Expected: `PASS`

---

### Task W3-T2: MCP 查询工具由角色配置驱动

**Files:**
- Modify: `internal/secretary/mcp_tools.go`
- Modify: `internal/secretary/mcp_tools_test.go`

**Depends on:** `[W3-T1]`

**Step 1: Write failing test**
```go
func TestMCPToolsFromRoleConfig(t *testing.T) {
    // roles.secretary.mcp.tools -> NewSessionRequest.MCPServers
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/secretary/ -run TestMCPToolsFromRoleConfig -count=1`
Expected: `missing role-driven mcp mapping`

**Step 3: Minimal implementation**
- `roles.secretary.mcp.enabled/tools` 转换为 `NewSessionRequest.MCPServers`
- 工具列表最小集合：`query_plans/query_plan_detail/query_pipelines/query_pipeline_logs/query_project_stats`

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/secretary/ -run TestMCPToolsFromRoleConfig -count=1`
Expected: `PASS`

---

### Task W3-T3: 文件变更检测统一走 ACP Handler

**Files:**
- Modify: `internal/secretary/acp_handler.go`
- Modify: `internal/secretary/acp_handler_test.go`

**Depends on:** `[W3-T1]`

**Step 1: Write failing test**
```go
func TestHandleWriteFilePublishesChangedEvent(t *testing.T) {
    // HandleWriteFile 成功后发 secretary_files_changed
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/secretary/ -run TestHandleWriteFilePublishesChangedEvent -count=1`
Expected: `undefined handler or event mismatch`

**Step 3: Minimal implementation**
- 路径归一化 + scope 校验
- `HandleWriteFile` 成功后发布 `secretary_files_changed`
- 记录变更文件清单到会话上下文

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/secretary/ -run TestHandleWriteFilePublishesChangedEvent -count=1`
Expected: `PASS`

---

### Task W3-T4: Review Orchestrator 按角色调用 ACP

**Files:**
- Create: `internal/secretary/review_orchestrator.go`
- Modify: `internal/secretary/review_test.go`

**Depends on:** `[W3-T1]`

**Step 1: Write failing test**
```go
func TestReviewOrchestratorUsesRoleBindings(t *testing.T) {
    // role_bindings.review_orchestrator.reviewers/aggregator 生效
    // reviewer/aggregator 默认复用会话
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/secretary/ -run TestReviewOrchestratorUsesRoleBindings -count=1`
Expected: `missing review_orchestrator path`

**Step 3: Minimal implementation**
- 组件命名统一为 `ReviewOrchestrator`
- 角色映射来源：`role_bindings.review_orchestrator`
- reviewer / aggregator 都按角色策略调用 ACP
- 默认 `roles[].session.reuse=true`，并注入 reset prompt

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/secretary/ -run TestReviewOrchestratorUsesRoleBindings -count=1`
Expected: `PASS`

---

### Task W3-T5: Web handlers 口径对齐

**Files:**
- Modify: `internal/web/handlers_chat.go`
- Modify: `internal/web/handlers_plan.go`
- Modify: `internal/web/handlers_chat_test.go`

**Depends on:** `[W3-T1, W3-T4]`

**Step 1: Write failing test**
```go
func TestChatSessionCreateWithRole(t *testing.T) {
    // POST /chat/sessions 支持 role 字段
}

func TestPlanReviewTriggersReviewOrchestrator(t *testing.T) {
    // plan review 入口走 ReviewOrchestrator
}
```

**Step 2: Run to confirm failure**
Run: `go test ./internal/web/ -run 'TestChatSessionCreateWithRole|TestPlanReviewTriggersReviewOrchestrator' -count=1`
Expected: `handler mismatch`

**Step 3: Minimal implementation**
- Chat session 创建参数使用 `role`
- Plan review 流程统一调用 `ReviewOrchestrator`
- WS 事件保持事件名不变，内部来源切到 ACP callback

**Step 4: Run tests to confirm pass**
Run: `go test ./internal/web/... -count=1 -v`
Expected: `PASS`

---

## Risks and Mitigations

| Risk | Severity | Mitigation |
|------|----------|------------|
| Secretary 会话恢复链复杂 | 高 | 强制覆盖 `LoadSession -> NewSession` 路径测试 |
| Review Orchestrator 复用引发审查漂移 | 中 | reset prompt + 结构化校验失败强制 fresh session |
| 文件回调 scope 校验遗漏 | 高 | 路径越界单测 + 归一化校验 |

## Wave Exit Gate
- Execute mandatory gate sequence via `executing-wave-plans`.
- Wave-specific acceptance:
  - [ ] Secretary 按 `secretary role` 运行并支持会话恢复
  - [ ] MCP 查询工具由角色配置驱动注册
  - [ ] 文件变更通过 `HandleWriteFile` 回调触发事件
  - [ ] Review Orchestrator 按 `review_orchestrator` 角色绑定运行，默认复用 reviewer + aggregator 会话
  - [ ] Web chat/plan handlers 走 role-driven ACP 路径
- Wave-specific verification:
  - [ ] `go test ./internal/secretary/... -count=1 -v` — PASS
  - [ ] `go test ./internal/web/... -count=1 -v` — PASS
  - [ ] `go build ./...` — PASS

## Next Wave Entry Condition
- Governed by `executing-wave-plans` verdict rule (`Go` / satisfied `Conditional Go` only).
