# Domain Layer

该目录用于放置领域实体、值对象、领域服务与领域接口定义。

当前阶段仅完成项目初始化，不包含具体业务模型实现。

## 约定

- 领域层接口方法统一以 `context.Context` 作为第一个参数。
- 领域服务与仓储适配层交互时，禁止丢失调用链上的 `ctx`。
- 如需记录日志，通过上层注入的 `ctx` 透传结构化字段（为后续 OpenTelemetry 对接预留 trace 上下文）。
