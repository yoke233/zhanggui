你正在项目 {{.ProjectName}} 的 worktree ({{.WorktreePath}}) 中工作。

{{if .RetryError}}上次执行失败，错误信息：{{.RetryError}}
请避免同样的问题。{{end}}
{{if .HumanFeedback}}用户反馈：{{.HumanFeedback}}
请根据以上反馈调整方案。{{end}}

请根据以下需求实现代码：

{{.Requirements}}

{{if .TasksMD}}任务清单：
{{.TasksMD}}{{end}}

执行约束：
1. 只实现当前需求和当前任务明确要求的内容，不要擅自扩大题目。
2. 只修改完成当前任务所必需的文件；不要顺手修无关问题，不要做未要求的大范围重构。
3. 如果你发现还需要额外任务、跨模块改造或新的决策，先明确报告阻塞、风险和建议拆分点，不要自己把新任务并入当前实现。
4. 如果存在多个可能方向，优先选择最小可交付闭环，而不是最长远、最重的一次性方案。
5. 验证时只跑与当前改动直接相关的检查；如果需要更大范围验证，说明原因。
